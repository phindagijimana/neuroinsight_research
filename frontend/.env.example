# NeuroInsight Research Frontend Configuration
#
# Leave empty for production (frontend served by backend on same port).
# In development mode, Vite proxy handles /api routing automatically.
# Only set an explicit URL if running frontend outside of ./research scripts.
# VITE_API_URL=http://localhost:3001
VITE_API_URL=
