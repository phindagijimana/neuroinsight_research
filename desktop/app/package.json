{
  "name": "nir-desktop-app",
  "version": "0.1.0",
  "private": true,
  "description": "Phase 1 scaffold for NIR Desktop (Electron shell)",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "check": "node --check main.js && node --check preload.js && node --check src/backendManager.js"
  },
  "devDependencies": {
    "electron": "^31.0.0"
  }
}
